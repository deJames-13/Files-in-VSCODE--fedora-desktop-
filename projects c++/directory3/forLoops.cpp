#include <iostream>
#include <cmath>
// if you want to unpack all the standard objects and functions
using namespace std;

int main()
{
    // for (condition){do something}
    for (int i = 10; i > 0; i--)
    {
        cout << i << endl;
    }
    return 0;
}